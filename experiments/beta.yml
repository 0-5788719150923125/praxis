# Beta blueprint configuration
# These defaults are applied when using --beta flag

seed: 42
device: cpu
batch_size: 1
target_batch_size: 64
block_size: 512
infer_context: 512
depth: 4
num_layers: 1
vocab_size: 4096
hidden_size: 128
embed_size: 96
ffn_type: arc
attention_type: arc
num_heads: 4
num_queries: 1
encoder_type: byte_latent_conv
tokenizer_type: byte_level
decoder_type: sequential
window_size: 64
activation: serpent
norm_type: sandwich
halting_type: kl
strategy: naive
tie_weights: true
schedule_free: true
train_datasets: [focused]
data_path: ["/opt/platformer"]
sampler_mode: uniform
task_weights: difficulty_bias_pretrain
loss_func: cross_entropy
head_type: crystal
tie_weights: true
