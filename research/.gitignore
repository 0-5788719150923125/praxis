*.pdf
!main.pdf

# Isolated latexmk output (aux, log, bbl, pdf) - the tracked main.pdf is only
# copied here on a clean build (see callbacks/lightning/paper.py).
build/

# Generated by `python -m praxis.pillars.build` from repository state
# (run logs, checkpoints, metrics, config) - reproducible, not committed.
variables.tex
data/
framing.tex
inlines.tex
geometries.tex
halting.tex
ghostmax.tex
title.tex
thread.tex
evolution_vars.tex
strands.tex
conjectures.tex
authors.tex
citations.bib
evolution.tex
figures/

# Stray top-level latex aux files (proper builds go to build/).
*.aux
*.bbl
*.blg
*.fls
*.log
*.fdb_latexmk
*.synctex.gz
datasets.tex
