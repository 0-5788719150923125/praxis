% Generated by praxis/pillars/evolution.py - do not edit by hand.
\newcommand{\paperEvolutionFigure}{%
\begin{figure}[tbp]
  \centering
  \includegraphics[width=0.9\linewidth]{figures/evolution.png}
  \caption{Praxis's git history as an isometric block field over the last 2209 commits: each block is one subsystem's line churn in one time window (time runs front-right toward HEAD, subsystem into depth, block height is churn). Blocks dim toward the past - the recency kernel the model applies to a sequence, turned on the repository. By recency-weighted churn the current center of gravity is core, web, encoders. The framework charts the hand that builds it.}
  \label{fig:evolution}
\end{figure}
}
