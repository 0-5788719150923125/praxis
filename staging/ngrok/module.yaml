name: "ngrok"
description: "Ngrok tunnel integration for exposing API server"
dependencies:
  python:
    - "ngrok"
    - "python-dotenv"
conditions:
  - "args.ngrok"
integrations:
  cli:
    function: "add_cli_args"
  api_server_hooks:
    function: "api_server_hook"
  request_middleware:
    function: "request_middleware"
  lifecycle:
    init: "initialize"
    cleanup: "cleanup"